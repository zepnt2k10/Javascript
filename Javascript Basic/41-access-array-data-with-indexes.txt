/*
Truy cập dữ liệu mảng với chỉ mục
Chúng ta có thể truy cập dữ liệu bên trong mảng bằng cách sử dụng các chỉ mục .

Chỉ mục mảng được viết trong cùng một ký hiệu trong ngoặc mà các chuỗi sử dụng, ngoại trừ việc thay vì chỉ định một ký tự, chúng đang chỉ định một mục nhập trong mảng. Giống như chuỗi, mảng sử dụng lập chỉ mục dựa trên 0 , vì vậy phần tử đầu tiên trong mảng có chỉ số là 0.


Ví dụ

const array = [50, 60, 70];
console.log(array[0]);
const data = array[1];
Các console.log(array[0])bản in 50, và datacó giá trị 60.

Tạo một biến được gọi myDatavà đặt nó bằng giá trị đầu tiên myArraybằng cách sử dụng ký hiệu dấu ngoặc.
*/
const myArray = [50, 60, 70];
const myData =myArray[0];

